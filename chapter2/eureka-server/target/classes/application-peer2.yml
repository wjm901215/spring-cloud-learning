server:
  port: 1112

eureka:
  instance:
    hostname: peer2
  client:
    #不对自己本身进行注册
    #register-with-eureka: false
    #维护服务实例，不检索服务
    #fetch-registry: false
    serviceUrl:
      defaultZone: http://peer1:1111/eureka